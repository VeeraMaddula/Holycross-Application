node_modules/
.env
data/db.json
data/db.backup-*.json
google-service-account.json

# OpenArt CLI credential file (src/openArt.js) — in production this lives on
# the persistent disk (PERSIST_DIR), never in the repo. Locally, with
# PERSIST_DIR unset, it falls back to a folder here — this is a live OAuth
# credential, never something to commit.
openart-home/

# Real staff/customer photos — not source code, shouldn't live in git history
public/img/avatars/
public/img/clock-selfies/

# Cash-safe accountability photos and report-evidence uploads — same reason,
# these are real, sensitive photos of staff/premises, not source code.
uploads/

# Throwaway manual test scripts (see each script's own header comment) —
# never meant to be committed.
test-sendmode.js
test-sendmode-nosenderid.js
