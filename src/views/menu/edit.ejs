<!DOCTYPE html>
<html>
<head><title>Menu & Events - Booking Admin</title><%- include('../partials/head') %></head>
<body>
<div class="app-shell">
  <%- include('../partials/sidebar') %>
  <div class="main">
    <h2>Menu &amp; Events</h2>

    <div class="card" style="overflow-x:visible">
      <h3 style="margin-top:0">Menu</h3>
      <form method="POST" action="/menu">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="field">
          <label>Intro line</label>
          <input type="text" name="intro" value="<%= menu.intro %>">
        </div>
        <% menu.sections.forEach((sec, i) => { %>
          <div class="menu-section">
            <div class="field">
              <label>Section title</label>
              <input type="text" name="sectionTitles[]" value="<%= sec.title %>">
            </div>
            <div class="menu-edit-grid">
              <% sec.items.forEach(item => { %>
                <div class="menu-edit-card">
                  <% if (item.photoUrl) { %>
                    <img class="menu-edit-photo" src="<%= item.photoUrl %>" alt="">
                  <% } else { %>
                    <div class="menu-edit-photo-placeholder">No photo</div>
                  <% } %>
                  <div class="menu-edit-fields">
                    <div class="menu-edit-row-inline">
                      <input type="text" name="title[<%= i %>][]" placeholder="Item name" value="<%= item.name %>">
                      <input type="text" name="prices[<%= i %>][]" placeholder="Price" value="<%= item.price %>">
                    </div>
                    <label class="menu-edit-label">Description</label>
                    <textarea name="descs[<%= i %>][]" placeholder="Description" rows="2"><%= item.desc %></textarea>
                    <label class="menu-edit-label">Allergens</label>
                    <textarea name="allergensField[<%= i %>][]" placeholder="Comma separated, e.g. Gluten, Egg, Milk" rows="2"><%= (item.allergens || []).join(', ') %></textarea>
                  </div>
                </div>
              <% }) %>
              <div class="menu-add-card">
                <div class="menu-add-card-label">Add item</div>
                <input type="text" name="title[<%= i %>][]" placeholder="New item name">
                <input type="text" name="prices[<%= i %>][]" placeholder="Price">
                <textarea name="descs[<%= i %>][]" placeholder="Description" rows="2"></textarea>
                <textarea name="allergensField[<%= i %>][]" placeholder="Allergens, comma separated" rows="2"></textarea>
              </div>
            </div>
          </div>
        <% }) %>
        <div class="menu-section">
          <div class="field">
            <label>New section title (leave blank to skip)</label>
            <input type="text" name="sectionTitles[]" placeholder="e.g. Desserts">
          </div>
          <div class="menu-edit-grid">
            <div class="menu-add-card">
              <div class="menu-add-card-label">First item in new section</div>
              <input type="text" name="title[<%= menu.sections.length %>][]" placeholder="Item name">
              <input type="text" name="prices[<%= menu.sections.length %>][]" placeholder="Price">
              <textarea name="descs[<%= menu.sections.length %>][]" placeholder="Description" rows="2"></textarea>
              <textarea name="allergensField[<%= menu.sections.length %>][]" placeholder="Allergens, comma separated" rows="2"></textarea>
            </div>
          </div>
        </div>
        <button class="btn" type="submit">Save menu</button>
      </form>
    </div>

    <div class="card">
      <h3 style="margin-top:0">Events</h3>
      <table>
        <thead><tr><th>Date</th><th>Title</th><th>Description</th><th></th></tr></thead>
        <tbody>
        <% events.forEach(e => { %>
          <tr>
            <td><%= e.date %></td>
            <td><%= e.title %></td>
            <td><%= e.description %></td>
            <td>
              <form class="inline" method="POST" action="/menu/events/<%= e.id %>/delete">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button class="btn small danger" type="submit">Remove</button>
              </form>
            </td>
          </tr>
        <% }) %>
        </tbody>
      </table>
      <h4>Add event</h4>
      <form method="POST" action="/menu/events">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="form-grid">
          <div class="field"><label>Title</label><input type="text" name="title" required></div>
          <div class="field"><label>Date</label><input type="date" name="date" required></div>
          <div class="field full"><label>Description</label><textarea name="description"></textarea></div>
        </div>
        <button class="btn" type="submit">Add event</button>
      </form>
    </div>
  </div>
</div>
</body>
</html>
